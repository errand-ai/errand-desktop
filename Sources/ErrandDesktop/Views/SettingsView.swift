import SwiftUI

/// Settings window for API keys, OIDC config, LiteLLM toggle, ports.
struct SettingsView: View {
    @EnvironmentObject var appState: AppState

    var body: some View {
        Text("TODO: Implement in task 2.4")
    }
}
